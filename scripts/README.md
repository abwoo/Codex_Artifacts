# Legacy Scripts

These PowerShell scripts are the original local-file MVP and are kept only as compatibility/reference material. The supported implementation is the Node/TypeScript service exposed through `artifact.cmd` and `npm run serve`.
